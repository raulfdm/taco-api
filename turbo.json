{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/app.js"],
      "env": ["NODE_ENV=production"]
    },
    "db:generate": {},
    "dev": {
      "env": ["NODE_ENV=development"]
    },
    "lint": {},
    "lint:ci": {},
    "start": {
      "dependsOn": ["build"]
    }
  }
}
