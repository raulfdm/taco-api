name: Website Docs

on: [push]

permissions:
  pull-requests: write

jobs:
  website:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout to repository
        uses: actions/checkout@v4.1.1

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: 1.0.20

      - name: Install dependencies
        run: bun install --no-cache

      - name: Add Netlify CLI
        run: bun add -g netlify-cli@17.10.1

      - name: Build
        run: bun run build --filter=website

      - name: Deploy
        id: netlify_deploy
        run: |
          NETLIFY_PREVIEW_URL=$(netlify deploy --dir apps/website/dist --site ${{ secrets.NETLIFY_SITE_ID }} --filter taco-api --auth ${{ secrets.NETLIFY_AUTH_TOKEN }})
          echo "NETLIFY_PREVIEW_URL=$NETLIFY_PREVIEW_URL" >> "$GITHUB_OUTPUT"

      - name: Extract Preview URL
        id: extract_preview_url
        run: |
          echo ${{join(steps.netlify_deploy.outputs.*, '\n')}}

        # run: |
        # ${{join(steps.netlify_deploy.outputs.*, '\n')}}
        # ${{ steps.netlify_deploy.outputs }}
        # echo "${{ steps.netlify_deploy.outputs }}"
        # # NETLIFY_PREVIEW_URL=$(echo "${{ steps.netlify_deploy.outputs }}" | grep 'Website draft URL' | cut -d' ' -f4)
        # # echo "NETLIFY_PREVIEW_URL=$URL" >> "$GITHUB_OUTPUT"
      # - run: echo ${{ steps.extract_preview_url.outputs.NETLIFY_PREVIEW_URL }}

      # - name: Netlify Deploy
      #   id: netlify_deploy
      #   uses: jsmrcaga/action-netlify-deploy@v2.1.0
      #   with:
      #     install_command: "echo Skipping installing the dependencies"
      #     build_command: "echo Skipping building the web files"
      #     build_directory: ./apps/website/dist
      #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      #     deploy_alias: ${{ env.BRANCH_NAME }}
      #     NETLIFY_DEPLOY_TO_PROD: ${{ env.BRANCH_NAME == 'main' }}
      #     monorepo_package: website

      # - name: foo
      #   shell: bash
      #   run: |
      #     echo ${{ steps.netlify_deploy.outputs }}
      #     echo ${{ github.event_name}}

      # - uses: actions/github-script@v7
      #   env:
      #     NETLIFY_PREVIEW_URL: ${{ steps.netlify_deploy.outputs.NETLIFY_PREVIEW_URL }}
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       async function comment(){
      #         console.log(process.env)
      #         const result = await github.rest.repos.listPullRequestsAssociatedWithCommit({
      #           owner: context.repo.owner,
      #           repo: context.repo.repo,
      #           commit_sha: context.sha,
      #         })

      #         const issueNumber = result.data[0].number

      #         await github.rest.issues.createComment({
      #           issue_number: issueNumber,
      #           owner: context.repo.owner,
      #           repo: context.repo.repo,
      #           body: 'Preview URL: '
      #         })
      #       }

      #       comment()
